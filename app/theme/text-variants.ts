import { RFValue } from "react-native-responsive-fontsize";
import palette from "./palette";

export default {
    title: {
        fontSize: RFValue(20),
        fontWeight: '100',
        color: 'black'
    }
}